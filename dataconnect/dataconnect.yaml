specVersion: "v1beta"
serviceId: "AppUser"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "AppUser-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
